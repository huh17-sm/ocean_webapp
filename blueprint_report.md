# 🏗️ 프리다이빙 교육관리 시스템: 현황 분석 및 개선 계획 (Blueprint)

**작성일**: 2026년 2월 10일
**분석 대상**: 현재 프로젝트 코드 vs 설계서 v2.md

---

## 1. 현재 상태 분석

### 💾 데이터베이스 (Supabase)

현재 마이그레이션 파일들을 분석한 결과, 설계서 v2와 비교하여 **핵심 테이블 일부가 누락**되어 있거나 **임시 형태**로 구현되어 있습니다.

| 테이블명 | 현재 상태 | 설계서 일치도 | 비고 |
|---|---|---|---|
| `users` (profiles) | ✅ 구현됨 | 높음 | 기본 프로필 및 관리자 메모 필드 존재 |
| `classes` | ✅ 구현됨 | 높음 | `media_link` 컬럼 최근 추가됨 |
| `reservations` | ⚠️ **부분 구현** | **낮음** | `debriefing`이 단순 텍스트 컬럼으로 존재 (V2는 별도 테이블 필요) |
| `class_requests` | ✅ 구현됨 | 높음 | 요청 상태 관리 등 구현됨 |
| `certification_requests`| ⚠️ **변경 필요** | 중간 | 설계서의 `certificates` 테이블 역할을 일부 수행 중이나 구조 차이 있음 |
| `courses` | ✅ 구현됨 | 높음 | 교육 과정 정보 테이블 존재 |
| `class_type_settings` | ✅ 구현됨 | - | 크레딧 비용 설정을 위한 테이블 (설계서엔 없지만 유용함) |
| **`debriefings`** | ❌ **없음** | **0%** | 설계서의 핵심인 상세 피드백(4가지 항목) 테이블 누락 |
| **`course_progress`** | ❌ **없음** | **0%** | 교육 진도율 관리 테이블 누락 |
| **`skill_completions`** | ❌ **없음** | **0%** | 스킬 체크리스트(스태틱, 다이나믹 등) 테이블 누락 |

### ⚡️ 기능 및 코드 구조

**주요 기능 현황:**
- **대시보드**: 예약 목록, 수업 요청, 크레딧 내역, 자격증 신청 현황 **구현 완료**.
- **관리자**: 수업 관리, 사용자 관리, 크레딧 관리 **구현 완료**.
- **교육 관리**: ❌ **미구현**. 진도율 확인이나 스킬 체크 기능이 아예 없습니다.
- **피드백**: 수업 완료 후 단순 텍스트 피드백만 가능합니다. (상세 피드백 불가)

**코드 구조:**
- `Next.js 15` (App Router) + `Supabase` 조합으로 구조가 잘 잡혀 있습니다.
- `src/app/admin/actions.ts` 등에 서버 액션이 집중되어 있어 리팩토링이 필요할 수 있습니다. (`actions/` 폴더로 분리 진행 중인 점은 긍정적)

---

## 2. 설계서 v2와의 차이점 (Gap Analysis)

### 🚨 누락된 핵심 항목 (Critical Missing)
1.  **상세 디브리핑 시스템 (`debriefings` 테이블)**
    *   **설계서**: 수행 평, 개선점, 장점, 다음 목표 4가지로 나누어 체계적 관리
    *   **현재**: `reservations` 테이블의 `debriefing` 컬럼 하나에 텍스트로 퉁침
2.  **교육 진도 관리 (`course_progress`, `skill_completions`)**
    *   **설계서**: "입문 과정 80% 달성", "스태틱 통과/다이나믹 미통과" 등 상세 추적
    *   **현재**: 없음. 사용자가 자신이 어디까지 배웠는지 알 수 없음.
3.  **마이페이지 - 내 진도 탭**
    *   **설계서**: 진도율 그래프와 스킬 체크리스트 표시
    *   **현재**: 예약 내역만 보임

### ⚠️ 다르게 구현된 항목
*   **자격증 관리**: 현재 `certification_requests` 테이블을 사용 중이나, 설계서의 `certificates` 테이블과 통합하여 **발급 이력**과 **신청 상태**를 명확히 관리할 필요가 있습니다.
*   **출석 체크**: 현재 `reservations`의 `status='attended'`로 처리 중이나, 설계서의 **강사 일괄 처리(Bulk Action)** UI가 없습니다.

---

## 3. 개선 작업 목록 (우선순위별)

### 🥇 우선순위 1 - 핵심 데이터 구조 정립 (Foundation)
가장 시급한 것은 DB 구조를 설계서에 맞추는 것입니다. 데이터가 쌓이기 전에 해야 합니다.

- [ ] **DB: `debriefings` 테이블 생성**
    - **현재 상태**: `reservations.debriefing` (텍스트)
    - **목표 상태**: `debriefings` 테이블 (1:1 관계, 4개 상세 필드)
    - **작업 내용**: 마이그레이션 파일 생성, 기존 데이터 마이그레이션 스크립트 작성
    - **영향 파일**: `supabase/migrations/*`, `types/index.ts`
    - **난이도**: 중

- [ ] **DB: `course_progress` 및 `skill_completions` 테이블 생성**
    - **현재 상태**: 없음
    - **목표 상태**: 교육 과정 진도와 스킬 달성 여부 저장
    - **작업 내용**: 마이그레이션 파일 생성
    - **영향 파일**: `supabase/migrations/*`
    - **난이도**: 하

- [ ] **Admin: 강사 전용 수업 관리 (일괄 처리)**
    - **현재 상태**: 개별 예약 수정만 가능
    - **목표 상태**: 한 수업의 수강생 목록을 띄우고 출석/피드백을 한 번에 처리
    - **작업 내용**: `saveDebriefingBulk` 액션 및 모달 UI 구현 (이전 작업 복구 및 개선)
    - **영향 파일**: `src/app/admin/actions.ts`, `src/components/admin/BulkDebriefingDialog.tsx`
    - **난이도**: 상

### 🥈 우선순위 2 - 사용자 경험 개선 (User Experience)
사용자가 체감할 수 있는 핵심 가치(상세 피드백, 진도 확인)를 제공합니다.

- [ ] **User: 마이페이지 - 상세 피드백 카드 (DebriefingCardV2)**
    - **현재 상태**: 단순 텍스트 표시
    - **목표 상태**: 4분할된 예쁜 디자인의 피드백 카드 표시
    - **작업 내용**: `DebriefingCardV2` 컴포넌트 구현 및 대시보드 연동
    - **영향 파일**: `src/components/dashboard/DebriefingCardV2.tsx`, `src/app/dashboard/page.tsx`
    - **난이도**: 중

- [ ] **User: 마이페이지 - 내 진도 현황 (My Progress)**
    - **현재 상태**: 없음
    - **목표 상태**: 레벨별 진도율(%)과 스킬 체크리스트 표시
    - **작업 내용**: 진도 데이터 Fetching 로직 및 UI 구현
    - **영향 파일**: `src/app/dashboard/page.tsx`, `src/components/dashboard/ProgressSection.tsx`
    - **난이도**: 상

### 🥉 우선순위 3 - 관리 및 안정화 (Admin & Stability)

- [ ] **Admin: 학생 상세 정보 페이지 개선**
    - **작업 내용**: 관리자가 학생의 스킬 하나하나를 체크해줄 수 있는 기능 추가
    - **난이도**: 중

---

## 4. 주의사항 및 리스크

1.  **기존 데이터 마이그레이션**:
    *   이미 `reservations.debriefing`에 쌓인 데이터가 있다면, 새 `debriefings` 테이블로 옮겨야 합니다. 현재 개발 초기 단계라 데이터가 적다면 **수동 이관**하거나 **초기화**를 고려할 수 있습니다.
2.  **호환성**:
    *   기존 코드가 `reservations.debriefing`을 참조하는 곳이 많습니다(`ReservationList.tsx` 등). 이를 모두 찾아 수정해야 에러가 나지 않습니다.

---

## 5. 권장 작업 순서

안전하고 효율적인 개발을 위해 다음 순서를 제안합니다.

**Step 1. DB 구조 변경 (가장 중요)**
`debriefings`, `course_progress`, `skill_completions` 테이블을 먼저 만듭니다.

**Step 2. 강사 도구 구현**
강사가 데이터를 '입력'할 수 있어야 합니다. `BulkDebriefingDialog`를 다시 구현하여 상세 피드백을 저장할 수 있게 합니다.

**Step 3. 사용자 화면 구현**
입력된 데이터를 사용자가 '조회'할 수 있게 합니다. 대시보드에 피드백 카드와 진도율 섹션을 추가합니다.

**Step 4. 기존 코드 정리**
더 이상 쓰지 않는 `reservations` 테이블의 텍스트 컬럼 관련 코드를 정리합니다.
